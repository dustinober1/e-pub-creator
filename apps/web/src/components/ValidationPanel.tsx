export function ValidationPanel() {
  return (
    <section className="panel validation-panel" aria-labelledby="validation-heading">
      <h2 id="validation-heading">Validation</h2>
      <p className="validation-status">No blocking issues found.</p>
    </section>
  );
}
